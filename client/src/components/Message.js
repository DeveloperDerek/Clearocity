import React from "react";

const Message = ({ message }) => {
    return(
        <section>
            <h1>test</h1>
            <p>message : {message}</p>
        </section>
    )
};

export default Message